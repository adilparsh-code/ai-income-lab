# Prisma 8 migrations

This directory is the Prisma ORM 8 migration history for AI Income Lab.

Application migrations live under `migrations/app/` and contract snapshots under `migrations/snapshots/`.

The legacy Prisma ORM 7 migration history remains under `prisma/migrations/` while the runtime migration is being transitioned safely to Prisma ORM 8.
